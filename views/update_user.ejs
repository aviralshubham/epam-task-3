
<!-- include header -->
<%- include('include/_header') %>
<!-- /include header -->

  <!-- Main Site -->
  <main id="site-main">
    <div class="container">
        <div class="box-nav d-flex justify-between">
           <div class="filter">
               <a href="/"><i class="fas fa-angle-double-left"></i> All Books</a>
           </div>
        </div>
        <div class="form-title text-center">
            <h2 class="text-dark">Update Issued Book</h2>
            <span class="text-light">Use the below form to Update Issued Book Details</span>
        </div>

        <!-- add user form -->
         <!-- form handling -->
 <form method="POST" id="update_user">
    <div class="new_user">
        <div class="form-group">
            <label for="name" class="text-light">Name</label>
            <input type="hidden" name="id" value="<%= user._id %>">
            <input type="text" name="name" value="<%= user.name %>" placeholder="Mark">
        </div>
        <div class="form-group">
            <label for="Email" class="text-light">Book Author</label>
            <input type="text" name="email" value="<%= user.email%>" placeholder="Author Name">
        </div>
        <div class="form-group">
            <label for="Price" class="text-light">Book Price</label>
            <input type="text" name="price" value="<%= user.price%>" placeholder="100">
        </div>
        <div class="form-group">
            <label for="gender" class="text-light">Study</label>
            <div class="radio inline">
                <input type="radio" id="radio-2" name="gender" value="Hosteler" <%= user.gender == 'Male' ? 'checked' : '' %>>
                <label for="radio-2" class="radio-label">Hosteler</label>
            </div>
            <div class="radio inline">
                <input type="radio" id="radio-3" name="gender" value="Day Scholar"  <%= user.gender == 'Female' ? 'checked' : '' %> >
                <label for="radio-3" class="radio-label">Day Scholar</label>
            </div>
        </div>

        <div class="form-group">
            <label for="gender" class="text-light">Status</label>
            <div class="radio inline">
                <input type="radio" id="radio-4" name="status" value="Taken" <%= user.status == 'Active' ? 'checked' : '' %> >
                <label for="radio-4" class="radio-label">Taken</label>
            </div>
            <div class="radio inline">
                <input type="radio" id="radio-5" name="status" value="Given" <%= user.status == 'Inactive' ? 'checked' : '' %> >
                <label for="radio-5" class="radio-label">Given</label>
            </div>
        </div>

        <div class="form-group">
            <button type="submit" class="btn text-dark update">Save</button>
        </div>

    </div>
</form>
       
    </div>
</main>
<!-- /Main Site -->

<!-- include footer -->
<%- include('include/_footer') %>
<!-- /include footer -->
